[project]
name = "plotviz"
version = "0.1.0"
description = ""
authors = [
    {name = "afj8z",email = "aidflem@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib (>=3.10.7,<4.0.0)",
    "numpy (>=2.3.5,<3.0.0)"
]

[tool.poetry]
packages = [{include = "plotviz", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
